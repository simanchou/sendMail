# sendMail
send email cli


```
[siman@dev sendMail]$ ./sendMail 
   Usage of ./sendMail:
     -b string
       	mail message body
     -spp string
       	smtp provider password
     -spu string
       	smtp provider user
     -t string
       	mail title
     -to string
       	send to someone
```